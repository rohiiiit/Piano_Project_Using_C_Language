# Piano_Project_Using_C_Language
This project firstly store the ASCII value of key pressed by user during runtime to generate sound. “dos.h” header file plays a vital role in performing tasks. The “sound()” will produce sound from pc speaker and in bracket we need to set frequency at which it will buzz.
